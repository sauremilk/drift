/**
 * Phase 2 stub — natural-language explanation of a signal.
 *
 * TODO(phase-2): Call drift.explain(signal_id) via Pyodide and render
 * the explanation alongside the finding cards.
 *
 * Requires:
 * - drift_explain MCP/API to be callable from browser Pyodide context
 * - Markdown renderer (e.g. react-markdown) for the explanation text
 */

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function ExplainPanel(_props: { abbrev: string }): null {
  // Phase 2 stub — not yet implemented
  return null;
}
